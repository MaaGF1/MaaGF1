name: check

on:
  push:
    branches: ["**"]
    paths:
      - ".github/workflows/check.yml"
      - "assets/**"
      - "mk/**"
      - "**.py"
  pull_request:
    branches: ["**"]
    paths:
      - ".github/workflows/check.yml"
      - "assets/**"
      - "mk/**"
      - "**.py"
  workflow_dispatch:

jobs:
  resource:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install maafw
        run: |
            python -m pip install --upgrade pip
            python -m pip install --upgrade maafw==5.0.1

      - name: Check Resource
        run: |
            python ./mk/check.py