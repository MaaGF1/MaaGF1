name: Build and Release

on:
  push:
    tags:
      - "v*"
    branches:
      - "**"
    paths:
      - ".github/workflows/install.yml"
      - "assets/**"
      - "**.py"
  pull_request:
    branches:
      - "**"
    paths:
      - ".github/workflows/install.yml"
      - "assets/**"
      - "**.py"
  workflow_dispatch:

permissions:
  contents: write
  issues: read
  pull-requests: read

jobs:
  meta:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - id: set_tag
        run: |
          is_release=${{ startsWith(github.ref, 'refs/tags/v') }}
          tag=$(git describe --tags --match "v*" ${{ github.ref }} || true)
          if [[ $tag != v* ]]; then
            tag=$(curl -sX GET "https://api.github.com/repos/${{ github.repository }}/releases/latest" --header 'authorization: Bearer ${{ secrets.GITHUB_TOKEN }}' | awk '/tag_name/{print $4}' FS='["]')
            if [[ $tag != v* ]]; then
              tag="v0.0.0"
            fi
            tag=$(date "+$tag-%y%m%d-$(git rev-parse --short HEAD)")
          fi
          if ! $($is_release) ; then
            prefix=${tag%-*-*}
            suffix=${tag#$prefix-}
            tag="$prefix-ci.$suffix"
          fi

          echo tag=$tag | tee -a $GITHUB_OUTPUT
          echo is_release=$is_release | tee -a $GITHUB_OUTPUT
    outputs:
      tag: ${{ steps.set_tag.outputs.tag }}
      is_release: ${{ steps.set_tag.outputs.is_release }}

  install:
    needs: meta
    runs-on: windows-latest
    strategy:
      matrix:
        os: [win]
        # MFAAvalonia naming convention usually uses 'x64' or 'arm64'
        arch: [x86_64, aarch64]
      fail-fast: false

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Python dependencies
        shell: bash
        run: |
          python -m pip install --upgrade pip
          pip install requests

      - name: Get MFAAvalonia release info
        id: mfa_info
        shell: bash
        run: |
          echo "Getting SweetSmellFox/MFAAvalonia latest release..."
          
          response=$(curl -sX GET "https://api.github.com/repos/SweetSmellFox/MFAAvalonia/releases/latest" --header 'authorization: Bearer ${{ secrets.GITHUB_TOKEN }}')
          
          tag=$(echo "$response" | jq -r '.tag_name // "unknown"')
          date=$(echo "$response" | jq -r '.published_at // "unknown"')
          url=$(echo "$response" | jq -r '.html_url // "unknown"')
          prerelease=$(echo "$response" | jq -r '.prerelease // false')
          
          echo "MFAAvalonia Info:"
          echo "  Tag: $tag"
          echo "  Date: $date"
          
          # Handle empty values
          tag=${tag:-"No releases"}
          date=${date:-"N/A"}
          url=${url:-""}
          
          echo "mfa_tag=$tag" >> $GITHUB_OUTPUT
          echo "mfa_date=$date" >> $GITHUB_OUTPUT
          echo "mfa_url=$url" >> $GITHUB_OUTPUT
          echo "mfa_prerelease=$prerelease" >> $GITHUB_OUTPUT

      - name: Download MFAAvalonia
        id: download_mfa
        uses: robinraju/release-downloader@v1
        with:
          repository: SweetSmellFox/MFAAvalonia
          # Matches MFAAvalonia-v1.8.0-win-x64.zip or similar
          fileName: "MFAAvalonia-*-${{ matrix.os }}-${{ (matrix.arch == 'x86_64' && 'x64') || (matrix.arch == 'aarch64' && 'arm64') }}*"
          latest: true
          out-file-path: "deps/bin"
          extract: true

      - name: Clean up MFAAvalonia archive
        shell: bash
        run: |
          # Remove the zip file to prevent it from being copied by install.py if logic changes
          find deps/bin -name "*.zip" -type f -delete
          echo "Archive cleanup completed."

      - name: Run Install Script
        shell: bash
        run: |
          # install.py cleans 'install/', copies 'deps/bin' content, and adds assets
          python ./install.py ${{ needs.meta.outputs.tag }}

      - name: Generate Build Report and Summary
        shell: bash
        run: |
          # Get build information
          BUILD_DATE=$(date -u '+%Y-%m-%d %H:%M:%S UTC')
          
          MFA_TAG="${{ steps.mfa_info.outputs.mfa_tag }}"
          MFA_DATE="${{ steps.mfa_info.outputs.mfa_date }}"
          MFA_URL="${{ steps.mfa_info.outputs.mfa_url }}"
          MFA_PRERELEASE="${{ steps.mfa_info.outputs.mfa_prerelease }}"

          # --- GitHub Step Summary ---
          cat >> $GITHUB_STEP_SUMMARY << 'SUMMARY_EOF'
          # 🚀 Build Report - ${{ matrix.arch }}
          
          ## 📋 Build Information
          | Component | Details |
          |-----------|---------|
          | **Build Date** | `BUILD_DATE_PLACEHOLDER` |
          | **Build Tag** | `${{ needs.meta.outputs.tag }}` |
          | **Architecture** | `${{ matrix.arch }}` |
          | **OS** | `${{ matrix.os }}` |
          | **Is Release** | ${{ needs.meta.outputs.is_release == 'true' && '✅ Yes' || '❌ No' }} |
          
          ## 📦 Dependencies (MFAAvalonia)
          | Property | Value |
          |----------|-------|
          | **Version** | MFA_VERSION_PLACEHOLDER |
          | **Release Date** | `MFA_DATE_PLACEHOLDER` |
          | **Type** | MFA_PRERELEASE_PLACEHOLDER |
          
          ## 🔗 Links
          - [MFAAvalonia](MFA_URL_PLACEHOLDER)
          
          ---
          *📅 Generated on BUILD_DATE_PLACEHOLDER*
          SUMMARY_EOF
          
          # Replacements for Summary
          sed -i "s/BUILD_DATE_PLACEHOLDER/${BUILD_DATE}/g" $GITHUB_STEP_SUMMARY
          sed -i "s/MFA_DATE_PLACEHOLDER/${MFA_DATE}/g" $GITHUB_STEP_SUMMARY
          
          if [[ -n "$MFA_URL" && "$MFA_URL" != "" ]]; then
            sed -i "s|MFA_VERSION_PLACEHOLDER|[${MFA_TAG}](${MFA_URL})|g" $GITHUB_STEP_SUMMARY
            sed -i "s|MFA_URL_PLACEHOLDER|${MFA_URL}|g" $GITHUB_STEP_SUMMARY
          else
            sed -i "s/MFA_VERSION_PLACEHOLDER/${MFA_TAG}/g" $GITHUB_STEP_SUMMARY
            sed -i "s|MFA_URL_PLACEHOLDER|#|g" $GITHUB_STEP_SUMMARY
          fi
          
          if [[ "$MFA_PRERELEASE" == "true" ]]; then
            sed -i "s/MFA_PRERELEASE_PLACEHOLDER/🚧 Pre-release/g" $GITHUB_STEP_SUMMARY
          else
            sed -i "s/MFA_PRERELEASE_PLACEHOLDER/✅ Stable/g" $GITHUB_STEP_SUMMARY
          fi

          # --- BUILD_REPORT.md (Inside install folder) ---
          cat > install/BUILD_REPORT.md << 'EOF'
          # Build Report
          
          ## Build Information
          | Component | Details |
          |-----------|---------|
          | **Build Date** | `BUILD_DATE_PLACEHOLDER` |
          | **Build Tag** | `${{ needs.meta.outputs.tag }}` |
          | **Architecture** | `${{ matrix.arch }}` |
          | **OS** | `${{ matrix.os }}` |
          
          ## Dependencies
          ### MFAAvalonia
          | Property | Value |
          |----------|-------|
          | **Version** | MFA_VERSION_PLACEHOLDER |
          | **Release Date** | `MFA_DATE_PLACEHOLDER` |
          | **Type** | MFA_PRERELEASE_PLACEHOLDER |
          
          ## Links
          - [MFAAvalonia Release](MFA_URL_PLACEHOLDER)
          
          ---
          *Generated by GitHub Actions.*
          EOF
          
          # Replacements for File
          sed -i "s/BUILD_DATE_PLACEHOLDER/${BUILD_DATE}/g" install/BUILD_REPORT.md
          sed -i "s/MFA_DATE_PLACEHOLDER/${MFA_DATE}/g" install/BUILD_REPORT.md
          
          if [[ -n "$MFA_URL" && "$MFA_URL" != "" ]]; then
            sed -i "s|MFA_VERSION_PLACEHOLDER|[${MFA_TAG}](${MFA_URL})|g" install/BUILD_REPORT.md
            sed -i "s|MFA_URL_PLACEHOLDER|${MFA_URL}|g" install/BUILD_REPORT.md
          else
            sed -i "s/MFA_VERSION_PLACEHOLDER/${MFA_TAG}/g" install/BUILD_REPORT.md
            sed -i "s/MFA_URL_PLACEHOLDER/#/g" install/BUILD_REPORT.md
          fi
          
          if [[ "$MFA_PRERELEASE" == "true" ]]; then
            sed -i "s/MFA_PRERELEASE_PLACEHOLDER/🚧 Pre-release/g" install/BUILD_REPORT.md
          else
            sed -i "s/MFA_PRERELEASE_PLACEHOLDER/✅ Stable/g" install/BUILD_REPORT.md
          fi
          
          echo "✅ Build report generated."

      - uses: actions/upload-artifact@v4
        with:
          name: MaaGF1-GUI-${{ needs.meta.outputs.tag }}-${{ matrix.arch }}
          path: "install"

  build-summary:
    if: always()
    needs: [meta, install]
    runs-on: ubuntu-latest
    steps:
      - name: Generate Overall Build Summary
        run: |
          BUILD_DATE=$(date -u '+%Y-%m-%d %H:%M:%S UTC')
          
          cat >> $GITHUB_STEP_SUMMARY << 'SUMMARY_EOF'
          # 🏁 Overall Build Summary
          
          ## 📊 Build Results
          | Architecture | Status | 
          |--------------|--------|
          | **aarch64** | ${{ contains(needs.install.result, 'success') && '✅ Success' || '❌ Failed' }} |
          | **x86_64** | ${{ contains(needs.install.result, 'success') && '✅ Success' || '❌ Failed' }} |
          
          ## 📋 Info
          - **Tag:** `${{ needs.meta.outputs.tag }}`
          - **Release:** ${{ needs.meta.outputs.is_release == 'true' && '✅ Yes' || '❌ No' }}
          - **Date:** `BUILD_DATE_PLACEHOLDER`
          
          ${{ needs.meta.outputs.is_release == 'true' && '## 🎉 Release Build' || '## 🔧 CI Build' }}
          
          ---
          SUMMARY_EOF
          
          sed -i "s/BUILD_DATE_PLACEHOLDER/${BUILD_DATE}/g" $GITHUB_STEP_SUMMARY

  changelog:
    name: Generate changelog
    runs-on: ubuntu-latest
    outputs:
      release_body: ${{ steps.git-cliff.outputs.content }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Generate a changelog
        uses: orhun/git-cliff-action@v4
        id: git-cliff
        with:
          config: .github/cliff.toml
          args: -vv --latest --strip header
        env:
          OUTPUT: CHANGES.md
          GITHUB_REPO: ${{ github.repository }}

  release:
    if: ${{ needs.meta.outputs.is_release == 'true' }}
    needs: [install, changelog]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: assets

      - run: |
          cd assets
          for f in *; do
            (cd $f && zip -r ../$f-${{ needs.meta.outputs.tag }}.zip .)
          done

      - uses: softprops/action-gh-release@v2
        with:
          files: assets/*
          tag_name: ${{ needs.meta.outputs.tag }}
          body: ${{ needs.changelog.outputs.release_body }}
          draft: false
          generate_release_notes: true
          prerelease: ${{ contains(needs.meta.outputs.tag, '-alpha') || contains(needs.meta.outputs.tag, '-beta') || contains(needs.meta.outputs.tag, '-rc') || contains(needs.meta.outputs.tag, '-pre') || contains(needs.meta.outputs.tag, '-dev') }}

      - name: Release Summary
        run: |
          cat >> $GITHUB_STEP_SUMMARY << 'EOF'
          # 🎉 Release Published!
          Release Tag: `${{ needs.meta.outputs.tag }}`
          EOF